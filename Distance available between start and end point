#include <stdio.h>
int main()
{
    int a[100],n,i,j,start,end;
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
    }
    scanf("%d",&start);
    scanf("%d",&end);
    for(i=0;i<n;i++)
    {
        if(a[i]>start && a[i]<end)
        {
            printf("%d\t",a[i]);
        }
    }

    return 0;
}
